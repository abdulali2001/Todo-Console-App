[project]
name = "todo-app"
version = "0.1.0"
description = "A minimal, clean CLI todo manager using Python 3.13+"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"